# --- Secrets ---
.env
.env.local
.env.production
secrets/
*-firebase-adminsdk-*.json

# --- Local tool binaries ---
.local-bin/
.firebase/

# --- Geocoder cache (regenerable, ~MB scale) ---
data/geocode_cache.json

# --- Local databases ---
enrichment.db
*.sqlite
*.sqlite3

# --- Raw source data (large, region-provided, regenerate locally) ---
*.geojson
*.csv

# --- Build artifacts (regenerable via `python3 build.py`) ---
# app_template.html is a SOURCE file (the template build.py reads). Keep it.
# docs/ is the GitHub Pages publish folder — IT IS TRACKED IN GIT.
app.html
app_nashville_tn.html
app_baltimore_md.html
app_fayette_ky.html
dist/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
env/

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# --- Node (if we add JS tooling later) ---
node_modules/
package-lock.json
